# PostgreSQL (matches docker-compose.yml defaults)
DATABASE_URL="postgresql://collectordex:collectordex@localhost:5432/collectordex?schema=public"

# Auth.js — generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-long-random-secret"
AUTH_URL="http://localhost:3000"
NEXTAUTH_URL="http://localhost:3000"

# Demo credentials used by prisma/seed.ts (local development only)
DEMO_USER_EMAIL="demo@collectordex.local"
DEMO_USER_PASSWORD="collectordex"
